# AGENTS.md

本文件约束 `D:\胡国东\北辰` 目录及其子目录内的自动化编码代理行为。

## 1. 目标与范围

- 目标：在不破坏现有业务流程的前提下，交付可验证、可回滚、可维护的改动。
- 范围：前端（`src/`）+ 后端（`server/`）+ 部署脚本。
- 默认部署形态：Express 同时托管 API 与前端静态资源（`dist/`）。

## 2. 项目事实（必须遵守）

1. 当前数据库实现为 **SQLite（better-sqlite3）**，默认文件 `data/sampler-salary.sqlite`。
2. 当前认证为 **本地账号 + Cookie Session**，不是 Supabase Auth。
3. API 统一在 `/api/*` 下，鉴权中间件在 `server/_core/auth.ts`。
4. 后端运行入口为 `dist/server/index.js`，构建由根目录脚本驱动。
5. Docker 部署以根目录 `Dockerfile` + `compose.yaml` 为准，默认形态为 **单容器 + 单 SQLite 持久化卷**。
6. 使用 Docker 时，容器内数据库路径固定为 `/app/data/sampler-salary.sqlite`，禁止多副本共享写同一个 SQLite 文件。

## 3. 执行原则

- 优先小步修改，避免大面积无关重构。
- 优先复用现有工具函数与组件，不随意引入新依赖。
- 涉及行为调整时，先确认影响模块（采样员/任务/工时/薪酬/薪酬配置）。
- 文案、字段名、状态枚举改动要前后端一致。
- 对外说明必须基于代码现状，不得继续引用已下线的 Supabase 运行链路。

## 4. 代码风格与变更约束

- TypeScript 保持严格类型，不用 `any` 掩盖问题（除非已有历史包袱且有说明）。
- React 组件优先保持 shadcn/ui 现有模式与目录组织。
- SQL/表结构变更需同步更新初始化逻辑（`server/lib/db.ts`）与对应 API。
- 不修改与任务无关的构建产物、锁文件或 `dist/` 内容（除非用户明确要求）。

## 5. 验证要求

完成改动前至少执行并记录：

```bash
npm run lint
npm run build
```

如改动仅涉及文档，可跳过构建，但需在结果中明确写出“仅文档变更，未执行构建”。

## 6. 文档维护要求

当改动影响运行方式、环境变量、默认账号、部署流程时，必须同步更新：

- `README.md`
- 本文件（`AGENTS.md`）中对应约束

## 7. 输出要求

最终反馈使用简明结构：

1. 改了什么（文件级别）
2. 为什么改（对应风险或需求）
3. 如何验证（命令 + 结果）
4. 剩余风险或后续建议（如有）

---

如与更高优先级指令（system/developer/user）冲突，以更高优先级为准。
